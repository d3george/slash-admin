{
  // extend your base config so you don't have to redefine your compilerOptions
  "extends": "./tsconfig.json",
  "include": ["src/**/*", "types", "vite.config.ts", "vite-env.d.ts", "**/*.*js"],
  "exclude": ["node_modules", "dist"]
}
